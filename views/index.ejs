<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>User List</title>
  </head>
  <body>
    <h1>User list</h1>

    <% if(locals.users.length>0){%>
    <ul>
      <% users.forEach(user=>{ %>
      <li style="display: flex; gap: 10px">
        <span
          >ID: <%=user.id%> Name: <%=user.firstName%> <%=user.lastName%> </span
        ><a href="/<%=user.id%>/update">Update</a>
      </li>

      <% }) %>
    </ul>
    <%} else{ %>
    <p>Oops! No one's here yet. Be the first to add your name!</p>
    <%} %>
    <a href="/create">Create a user</a>
  </body>
</html>
